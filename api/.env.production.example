# Production Environment Configuration for Azure
# Copy this to .env.production when ready to deploy

# Database (Azure PostgreSQL - CREATED SUCCESSFULLY!)
DATABASE_URL="postgresql://procurement_admin:SecureProcurement2025!@procurement-postgres-db.postgres.database.azure.com/flexibleserverdb?sslmode=require"

# Azure Functions
FUNCTIONS_WORKER_RUNTIME=node
AzureWebJobsStorage=""

# JWT Secret (generate a new strong secret for production)
JWT_SECRET="generate-a-new-super-secret-jwt-key-for-production-change-this"

# CORS Origins (update with your Static Web App domain)
CORS_ORIGINS="https://your-static-web-app-name.azurestaticapps.net"

# Environment
NODE_ENV=production
